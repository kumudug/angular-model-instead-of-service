# angular-model-instead-of-service

Create a user object instead of service functions that know about itself.

This way you can instanciate an object using a constructor and use functions within the object
